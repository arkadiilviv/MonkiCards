<UserControl x:Class="Monki.Admin.Controls.DeckListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Monki.Admin.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
			 x:Name="root">
	<Border Padding="10">
		<Grid>
			<DataGrid ItemsSource="{Binding DecksListVm.Decks, ElementName=root}"
					  SelectedItem="{Binding DecksListVm.SelectedDeck,Mode=TwoWay, ElementName=root}"
					  AutoGenerateColumns="False"
					  IsReadOnly="True">
				<DataGrid.Columns>
					<DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="0.5*"/>
					<DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="1*"/>
					<DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
					<DataGridTextColumn Header="User" Binding="{Binding UserName}"  Width="1*"/>
				</DataGrid.Columns>
				<DataGrid.InputBindings>
					<MouseBinding MouseAction="LeftDoubleClick"
								  Command="{Binding DecksListVm.OpenDeckCommand, ElementName=root}"/>
				</DataGrid.InputBindings>
			</DataGrid>
		</Grid>
	</Border>
</UserControl>
